{
  "name": "C",
  "description": "A general-purpose, procedural computer programming language supporting structured programming.",
  "lessons": [
    {
      "title": "Basic Syntax",
      "description": "Learn about C basic syntax and structure",
      "content": [
        "#include <stdio.h>",
        "",
        "int main() {",
        "    // Output to console",
        "    printf(\"Hello, World!\\n\");",
        "    ",
        "    // Variables",
        "    int number = 42;",
        "    float pi = 3.14159f;",
        "    char grade = 'A';",
        "    ",
        "    printf(\"The value of number is: %d\\n\", number);",
        "    printf(\"The value of pi is: %f\\n\", pi);",
        "    printf(\"The grade is: %c\\n\", grade);",
        "    ",
        "    // Input from console",
        "    int userInput;",
        "    printf(\"Please enter a number: \");",
        "    scanf(\"%d\", &userInput);",
        "    printf(\"You entered: %d\\n\", userInput);",
        "    ",
        "    // Constants",
        "    const int MAX_VALUE = 1000;",
        "    printf(\"The maximum value is: %d\\n\", MAX_VALUE);",
        "    ",
        "    return 0;",
        "}"
      ]
    },
    {
      "title": "Functions and Structures",
      "description": "Practice typing C functions and structures",
      "content": [
        "#include <stdio.h>",
        "#include <string.h>",
        "",
        "// Function declaration",
        "int add(int a, int b);",
        "float multiply(float a, float b);",
        "",
        "// Structure definition",
        "struct Person {",
        "    char name[50];",
        "    int age;",
        "};",
        "",
        "// Function to display person information",
        "void introduce(struct Person p) {",
        "    printf(\"Hello, my name is %s and I am %d years old.\\n\", p.name, p.age);",
        "}",
        "",
        "int main() {",
        "    // Function calls",
        "    int sum = add(5, 3);",
        "    printf(\"Sum: %d\\n\", sum);",
        "    ",
        "    float product = multiply(2.5f, 3.0f);",
        "    printf(\"Product: %f\\n\", product);",
        "    ",
        "    // Creating and using structures",
        "    struct Person person1;",
        "    strcpy(person1.name, \"John\");",
        "    person1.age = 30;",
        "    introduce(person1);",
        "    ",
        "    struct Person person2;",
        "    strcpy(person2.name, \"Alice\");",
        "    person2.age = 25;",
        "    person2.age++; // Increment age",
        "    introduce(person2);",
        "    ",
        "    return 0;",
        "}",
        "",
        "// Function definitions",
        "int add(int a, int b) {",
        "    return a + b;",
        "}",
        "",
        "float multiply(float a, float b) {",
        "    return a * b;",
        "}"
      ]
    },
    {
      "title": "Control Flow",
      "description": "Practice typing C control flow statements",
      "content": [
        "#include <stdio.h>",
        "",
        "int main() {",
        "    // If-else statement",
        "    int score = 85;",
        "    ",
        "    if (score >= 90) {",
        "        printf(\"Grade: A\\n\");",
        "    } else if (score >= 80) {",
        "        printf(\"Grade: B\\n\");",
        "    } else if (score >= 70) {",
        "        printf(\"Grade: C\\n\");",
        "    } else if (score >= 60) {",
        "        printf(\"Grade: D\\n\");",
        "    } else {",
        "        printf(\"Grade: F\\n\");",
        "    }",
        "    ",
        "    // Switch statement",
        "    int day = 3;",
        "    char dayName[20];",
        "    ",
        "    switch (day) {",
        "        case 1:",
        "            strcpy(dayName, \"Monday\");",
        "            break;",
        "        case 2:",
        "            strcpy(dayName, \"Tuesday\");",
        "            break;",
        "        case 3:",
        "            strcpy(dayName, \"Wednesday\");",
        "            break;",
        "        case 4:",
        "            strcpy(dayName, \"Thursday\");",
        "            break;",
        "        case 5:",
        "            strcpy(dayName, \"Friday\");",
        "            break;",
        "        case 6:",
        "            strcpy(dayName, \"Saturday\");",
        "            break;",
        "        case 7:",
        "            strcpy(dayName, \"Sunday\");",
        "            break;",
        "        default:",
        "            strcpy(dayName, \"Invalid day\");",
        "            break;",
        "    }",
        "    ",
        "    printf(\"Day: %s\\n\", dayName);",
        "    ",
        "    // For loop",
        "    printf(\"Counting from 1 to 5:\\n\");",
        "    for (int i = 1; i <= 5; i++) {",
        "        printf(\"Count: %d\\n\", i);",
        "    }",
        "    ",
        "    // While loop",
        "    printf(\"Countdown from 5 to 1:\\n\");",
        "    int countdown = 5;",
        "    while (countdown > 0) {",
        "        printf(\"Countdown: %d\\n\", countdown);",
        "        countdown--;",
        "    }",
        "    ",
        "    return 0;",
        "}"
      ]
    }
  ]
}