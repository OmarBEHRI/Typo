{
  "name": "JavaScript",
  "description": "A high-level, interpreted programming language that conforms to the ECMAScript specification.",
  "lessons": [
    {
      "title": "Variables and Data Types",
      "description": "Learn about JavaScript variables and basic data types",
      "content": [
        "let name = 'John';",
        "const age = 30;",
        "var isActive = true;",
        "",
        "let number = 42;",
        "let decimal = 3.14;",
        "let text = 'Hello, world!';",
        "let array = [1, 2, 3, 4, 5];",
        "let object = { key: 'value', number: 42 };",
        "let nothing = null;",
        "let undefined_var;",
        "",
        "console.log(typeof number);  // 'number'",
        "console.log(typeof text);    // 'string'",
        "console.log(typeof isActive); // 'boolean'"
      ]
    },
    {
      "title": "Functions",
      "description": "Practice typing JavaScript functions",
      "content": [
        "// Function declaration",
        "function greet(name) {",
        "  return `Hello, ${name}!`;",
        "}",
        "",
        "// Function expression",
        "const multiply = function(a, b) {",
        "  return a * b;",
        "};",
        "",
        "// Arrow function",
        "const add = (a, b) => a + b;",
        "",
        "// Function with default parameters",
        "function createUser(name, age = 25, role = 'user') {",
        "  return {",
        "    name,",
        "    age,",
        "    role",
        "  };",
        "}",
        "",
        "// Immediately Invoked Function Expression (IIFE)",
        "(function() {",
        "  console.log('This function runs immediately');",
        "})();"
      ]
    },
    {
      "title": "Control Flow",
      "description": "Practice typing JavaScript control flow statements",
      "content": [
        "// If statement",
        "let temperature = 22;",
        "",
        "if (temperature > 30) {",
        "  console.log('It\\'s hot outside!');",
        "} else if (temperature > 20) {",
        "  console.log('It\\'s a nice day!');",
        "} else {",
        "  console.log('It\\'s cold outside!');",
        "}",
        "",
        "// Switch statement",
        "let day = 'Monday';",
        "",
        "switch (day) {",
        "  case 'Monday':",
        "    console.log('Start of the work week');",
        "    break;",
        "  case 'Friday':",
        "    console.log('End of the work week');",
        "    break;",
        "  case 'Saturday':",
        "  case 'Sunday':",
        "    console.log('Weekend!');",
        "    break;",
        "  default:",
        "    console.log('Midweek');",
        "    break;",
        "}",
        "",
        "// For loop",
        "for (let i = 0; i < 5; i++) {",
        "  console.log(`Iteration ${i}`);",
        "}",
        "",
        "// While loop",
        "let count = 0;",
        "while (count < 5) {",
        "  console.log(`Count: ${count}`);",
        "  count++;",
        "}"
      ]
    }
  ]
}